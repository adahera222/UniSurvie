//Ce script doit accéder aux infos lui permettant d'établir la Météo, comme l'heure de la journée, la saison etc.
using UnityEngine;
using System.Collections;

public class GestionMeteo : MonoBehaviour {
	
	private float _heureActuelle;
	private float _temperatureActuelle;
	private string _saisonActuelle;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
